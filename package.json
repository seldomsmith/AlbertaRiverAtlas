{
  "name": "alberta-rivers-monorepo",
  "version": "1.0.0",
  "description": "Alberta River Atlas — unified monorepo launcher",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "setup": "node scripts/setup-env.js",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:frontend",
    "start": "npm start --prefix backend",
    "dev": "node scripts/setup-env.js && npm run build --prefix frontend && npm start --prefix backend"
  },
  "private": true
}
