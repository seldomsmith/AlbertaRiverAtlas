{
  "name": "alberta-rivers-monorepo",
  "version": "1.0.0",
  "description": "Monorepo configuration with unified build and static execution scripts",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:frontend",
    "start": "npm start --prefix backend",
    "dev": "npm run build:frontend && npm start --prefix backend"
  },
  "private": true
}
